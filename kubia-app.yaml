apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: kubia
  namespace: argocd
spec:
  project: default

  source:
    repoURL: https://github.com/Shashwot023/argocd_test.git
    targetRevision: HEAD
    path: .

  destination:
    server: https://kubernetes.default.svc
    namespace: default

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
